import type { GameAST } from '../../ast/types.js';
/** Generate the BootScene class that preloads all assets and creates animations. */
export declare function generatePreloadScene(ast: GameAST): string;
//# sourceMappingURL=preloadTemplate.d.ts.map